#include <iostream>
using namespace std;
int a[55];
int main()
{
	int n,k;
	cin >> n >> k;
	for(int i=1;i<=n;i++)
		cin >> a[i];
	int x=a[k] , ans=0;
	for(int i=1;i<=n;i++)
		if(x==0)
		{
			if(a[i]>0)
				ans=i;
		}
		else
			if(a[i]>=x)
				ans[i];
	cout << ans << endl;
}

#include <iostream>
#include <map>

using namespace std;

map<int> k;

int main()
{
	int f;
	f=1;
	int v;
	v=3
	k.insert(f);
	k.insert(v);
	if(k.find(1)!=k.end())
	{
		cout << k.size() << " :O " << endl;
		cout << " :) " << endl;
	}
	else
		cout << " :| " << endl;
}
